## Tasks
- [x] 1. Update backend authorize route to honor `mode` query and default to minimal login scopes.
- [x] 2. Ensure frontend OAuth entry points propagate query parameters and use `mode=gmail` for mailbox consent.
- [x] 3. Verify login and Gmail connect flows (manual OAuth smoke + automated compile checks).
- [x] 4. Rebuild and restart affected containers after implementation.
